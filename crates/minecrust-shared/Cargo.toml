[package]
name = "minecrust-shared"
version = "0.1.0"
edition = "2024"

[dependencies]
bincode = "1.3.3"
serde = { version = "1.0.228", features = ["derive", "rc"] }
glam = { version = "0.32.1", features = ["serde"] }
hecs = "0.11.0"
noise = "0.9.0"
laminar = "0.5.0"
